# Database Configuration (RDS)
DB_HOST=your-rds-endpoint.rds.amazonaws.com
DB_PORT=5432
DB_PASSWORD=your_database_password
DB_USERNAME=postgres
DB_DATABASE=study_swipe
# Note: SSL certificate (global-bundle.pem) is automatically downloaded in CI/CD and Docker build

# Email Configuration
MAIL_FROM=no-reply@study-swipe.com

# AWS SES Configuration
AWS_REGION=ap-northeast-2
SES_ACCESS_KEY=your_ses_access_key
SES_SECRET_ACCESS_KEY=your_ses_secret_key

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_here
JWT_REFRESH_SECRET=your_jwt_refresh_secret_key_here

# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here
