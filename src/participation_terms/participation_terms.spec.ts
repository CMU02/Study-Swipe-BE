import { ParticipationTerms } from './participation_terms.entity';

describe('ParticipationTerms', () => {
  it('should be defined', () => {
    expect(new ParticipationTerms()).toBeDefined();
  });
});
