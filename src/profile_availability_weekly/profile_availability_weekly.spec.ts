import { ProfileAvailabilityWeekly } from './profile_availability_weekly.entity';

describe('ProfileAvailabilityWeekly', () => {
  it('should be defined', () => {
    expect(new ProfileAvailabilityWeekly()).toBeDefined();
  });
});
