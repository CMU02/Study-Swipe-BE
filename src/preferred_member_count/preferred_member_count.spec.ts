import { PreferredMemberCount } from './preferred_member_count.entity';

describe('PreferredMemberCount', () => {
  it('should be defined', () => {
    expect(new PreferredMemberCount()).toBeDefined();
  });
});
